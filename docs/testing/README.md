# SolidOA 回归测试指南

## 1. 测试文档概览

本目录包含 SolidOA 系统的完整回归测试方案：

| 文件 | 说明 |
|------|------|
| `REGRESSION_TEST_PLAN.md` | 完整测试用例文档（132个用例） |
| `Solidoa_Regression_Test.postman_collection.json` | Postman 可导入的 API 测试集合 |
| `README.md` | 本指南 |

## 2. Postman 集合使用指南

### 2.1 导入集合

1. 打开 Postman
2. 点击 `Import` 按钮
3. 选择 `Solidoa_Regression_Test.postman_collection.json` 文件
4. 集合将自动导入

### 2.2 配置环境变量

1. 创建新环境（如 `SolidOA Local`）
2. 添加以下变量：

| 变量名 | 初始值 | 说明 |
|--------|--------|------|
| `baseUrl` | `http://localhost:8080` | API 基础地址 |
| `adminToken` | 空 | 管理员 Token |
| `managerToken` | 空 | 部门经理 Token |
| `userToken` | 空 | 普通员工 Token |
| `financeToken` | 空 | 财务专员 Token |
| `hrToken` | 空 | 考勤专员 Token |
| `refreshToken` | 空 | 刷新 Token |
| `testUserId` | 空 | 测试用户ID |
| `testLeaveId` | 空 | 测试请假ID |
| `testExpenseId` | 空 | 测试报销ID |
| `testFileId` | 空 | 测试文件ID |
| `dingtalkCode` | 空 | 钉钉授权码 |

3. 设置环境为活动状态

### 2.3 执行测试

**方式一：按顺序执行认证用例（自动获取 Token）**
1. 展开 `0. 认证与授权` 文件夹
2. 选中 `TC-AUTH-001: 正确登录-管理员`
3. 点击 `Send` 获取 adminToken
4. 重复登录其他角色获取对应 Token
5. 运行后续用例

**方式二：使用 Collection Runner**
1. 点击集合右侧的 `Run collection` 按钮
2. 选择环境
3. 设置迭代次数和延迟
4. 点击 `Run`

### 2.4 集合结构

```
SolidOA API Regression Test Collection
├── 0. 认证与授权 (10个用例)
│   ├── 正确登录（各角色）
│   ├── 密码错误登录
│   ├── JWT刷新
│   ├── 登出
│   ├── 钉钉免登回调
│   └── 无Token访问
├── 1. 系统管理模块 (8个用例)
├── 2. 请假审批模块 (9个用例)
├── 3. 费用报销模块 (7个用例)
├── 4. 考勤打卡模块 (5个用例)
├── 5. 消息通知模块 (6个用例)
├── 6. 文件服务模块 (6个用例)
├── 7. 钉钉同步模块 (5个用例)
└── 8. 监控指标接口 (3个用例)
```

## 3. 测试用例覆盖范围

| 模块 | 用例数 | 关键路径 |
|------|--------|----------|
| 用户认证 | 16 | ✅ |
| 系统管理 | 18 | ✅ |
| 请假审批 | 20 | ✅ |
| 费用报销 | 18 | ✅ |
| 考勤打卡 | 16 | ✅ |
| 消息通知 | 16 | ✅ |
| 文件服务 | 16 | ✅ |
| 钉钉同步 | 12 | ✅ |
| **合计** | **132** | - |

## 4. 测试账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | Admin@123 | 系统管理员 |
| 部门经理 | manager | Manager@123 | 部门经理 |
| 普通员工 | user | User@123 | 普通员工 |
| 财务专员 | finance | Finance@123 | 财务审批 |
| 考勤专员 | hr | Hr@123 | 考勤管理 |

## 5. 测试前检查清单

- [ ] 所有服务已启动（Gateway, System, Workflow, Finance, Attendance, Collaboration, File, DingTalk）
- [ ] Nacos 服务正常
- [ ] MySQL、Redis、RabbitMQ、MinIO 运行正常
- [ ] 测试数据库已初始化
- [ ] Postman 环境变量已配置

## 6. 测试执行流程

### 6.1 完整回归测试（1-2小时）

1. **阶段1：认证测试**（约5分钟）
   - 执行 `0. 认证与授权` 下所有用例
   - 验证 Token 获取和刷新机制

2. **阶段2：系统管理测试**（约10分钟）
   - 执行 `1. 系统管理模块` 下所有用例
   - 验证权限校验

3. **阶段3：核心业务测试**（约30分钟）
   - 执行 `2. 请假审批模块`
   - 执行 `3. 费用报销模块`
   - 验证审批流程

4. **阶段4：其他模块测试**（约20分钟）
   - 执行 `4. 考勤打卡模块`
   - 执行 `5. 消息通知模块`
   - 执行 `6. 文件服务模块`
   - 执行 `7. 钉钉同步模块`

5. **阶段5：监控与报告**（约5分钟）
   - 执行 `8. 监控指标接口`
   - 生成测试报告

### 6.2 冒烟测试（15分钟）

快速验证核心功能：

1. 登录（admin, user）
2. 创建请假申请
3. 审批请假
4. 打卡
5. 上传文件
6. 发送消息

## 7. 测试结果记录

执行完成后，请记录：

1. **通过率**：通过用例数 / 总用例数
2. **失败用例**：记录失败的用例ID和原因
3. **需要人工验证的用例**：按人工验证清单验证

## 8. 人工验证清单

以下用例需要人工验证（无法自动化）：

| 用例ID | 场景 | 验证方式 |
|--------|------|----------|
| TC-AUTH-010 | 钉钉免登 | 扫码确认 |
| TC-AUTH-012 | 钉钉code replay | 重复使用code |
| TC-AUTH-013 | 钉钉回调签名 | 检查钉钉后台 |
| TC-MSG-006 | WebSocket连接 | 客户端测试 |
| TC-MSG-008 | WebSocket推送 | 接收消息 |
| TC-MSG-009 | 钉钉工作通知 | 检查钉钉客户端 |
| TC-DING-004 | 用户数据一致性 | 手动对比 |
| TC-DING-005 | 部门数据一致性 | 手动对比 |
| TC-DING-010 | 钉钉消息推送 | 检查钉钉客户端 |
| TC-ATTEND-001 | 实际打卡 | 实际位置打卡 |
| TC-ATTEND-003 | 超出范围打卡 | 移动到范围外 |

## 9. 缺陷报告

发现缺陷时，按以下格式记录：

```
缺陷编号：DEF-{模块}-{序号}
严重程度：P0/P1/P2/P3
测试用例：TC-XXX
标题：[简要描述]
描述：[详细描述]
复现步骤：
1. 
2. 
预期结果：
实际结果：
环境信息：系统版本、浏览器等
附件：截图、日志
```

---

*本文档由 Claude Code 自动生成 - 2026-05-24*