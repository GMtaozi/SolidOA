<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solidoa.hr.attendance.mapper.AttendanceHolidayMapper">

    <!-- 查询某年节假日列表 -->
    <select id="selectByYear" resultType="com.solidoa.hr.attendance.entity.AttendanceHoliday">
        SELECT * FROM oa_attendance_holiday
        WHERE year = #{year}
        ORDER BY holiday_date
    </select>

    <!-- 查询指定日期范围内的节假日 -->
    <select id="selectByDateRange" resultType="com.solidoa.hr.attendance.entity.AttendanceHoliday">
        SELECT * FROM oa_attendance_holiday
        WHERE holiday_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY holiday_date
    </select>

    <!-- 查询调休上班日 -->
    <select id="selectWorkdays" resultType="com.solidoa.hr.attendance.entity.AttendanceHoliday">
        SELECT * FROM oa_attendance_holiday
        WHERE year = #{year} AND holiday_type = 'WORKDAY'
        ORDER BY holiday_date
    </select>

    <!-- 查询节假日 -->
    <select id="selectHolidays" resultType="com.solidoa.hr.attendance.entity.AttendanceHoliday">
        SELECT * FROM oa_attendance_holiday
        WHERE year = #{year} AND holiday_type = 'HOLIDAY'
        ORDER BY holiday_date
    </select>

</mapper>