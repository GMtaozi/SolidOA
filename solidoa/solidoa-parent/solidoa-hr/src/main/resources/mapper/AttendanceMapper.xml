<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solidoa.hr.attendance.mapper.AttendanceMapper">

    <select id="selectLastRecord" resultType="com.solidoa.hr.attendance.entity.Attendance">
        SELECT * FROM oa_attendance
        WHERE user_id = #{userId} AND check_date = #{checkDate}
        ORDER BY check_time DESC LIMIT 1
    </select>

    <select id="selectPageList" resultType="com.solidoa.hr.attendance.vo.AttendanceVO">
        SELECT * FROM oa_attendance
        WHERE user_id = #{userId} AND check_date = #{checkDate}
        ORDER BY check_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM oa_attendance
        WHERE user_id = #{userId} AND check_date = #{checkDate}
    </select>

    <select id="selectByMonth" resultType="com.solidoa.hr.attendance.entity.Attendance">
        SELECT * FROM oa_attendance
        WHERE user_id = #{userId} AND DATE_FORMAT(check_date, '%Y-%m') = #{yearMonth}
        ORDER BY check_time
    </select>

    <select id="countDeviceUsersToday" resultType="int">
        SELECT COUNT(DISTINCT user_id) FROM oa_attendance
        WHERE device_id = #{deviceId} AND check_date = #{checkDate}
    </select>

    <!-- 按日期范围查询打卡记录 -->
    <select id="selectByDateRange" resultType="com.solidoa.hr.attendance.entity.Attendance">
        SELECT * FROM oa_attendance
        WHERE user_id = #{userId}
        AND check_date BETWEEN #{startDate} AND #{endDate}
        ORDER BY check_time DESC
    </select>

    <!-- 查询所有不同的用户ID -->
    <select id="selectDistinctUserIds" resultType="java.lang.Long">
        SELECT DISTINCT user_id FROM oa_attendance
    </select>

</mapper>