<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solidoa.system.mapper.ScheduleMapper">

    <select id="selectByUserIdAndDateRange" resultType="com.solidoa.system.entity.Schedule">
        SELECT * FROM oa_schedule
        WHERE user_id = #{userId}
          AND start_time &lt; #{endDate}
          AND end_time &gt; #{startDate}
        ORDER BY start_time ASC
    </select>

    <select id="selectByDateRange" resultType="com.solidoa.system.entity.Schedule">
        SELECT * FROM oa_schedule
        WHERE start_time &lt; #{endDate}
          AND end_time &gt; #{startDate}
        ORDER BY start_time ASC
    </select>

</mapper>