<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solidoa.workflow.mapper.ReminderRecordMapper">

    <select id="selectByBusinessAndDate" resultType="com.solidoa.workflow.entity.ReminderRecord">
        SELECT * FROM oa_reminder_record
        WHERE business_type = #{businessType}
          AND business_id = #{businessId}
          AND DATE(last_reminder_time) = #{date}
        LIMIT 1
    </select>

</mapper>